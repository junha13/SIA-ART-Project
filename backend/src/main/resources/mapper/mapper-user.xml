<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="lx.team2.backend.feature.user.UserMapper">

    <!-- ========== 로그인 ========== -->
    <select id="getUserIdByLogin" parameterType="LoginDTO" resultType="int">
        SELECT user_number
        FROM tb_user
        WHERE (id = #{loginId} OR email = #{loginId})
          AND password = #{loginPw}
    </select>

    <!-- ========== 회원가입 ========== -->
    <insert id="insertUser" parameterType="RegisterDTO">
        INSERT INTO tb_user (
            role_number, user_name, email, mobile, id, password, activity_name, interests_list
        )
        VALUES (
                   #{userType}, #{name}, #{email}, #{phone}, #{userId}, #{password}, #{nickname}, #{selectedInterests}
               )
    </insert>

    <!-- ========== ID 중복 확인 ========== -->
    <select id="checkDuplicateId" parameterType="String" resultType="int">
        SELECT COUNT(*)
        FROM tb_user
        WHERE id = #{userId}
    </select>

</mapper>
